package site.lifix.jiscord.api.statics.channel;

public class SortOrderType {
    // Sort by the most recent message in the thread
    public static final int LATEST_ACTIVITY = 0;

    // Sort by when the thread was created (from most recent to oldest)
    public static final int CREATION_TIME = 1;
}

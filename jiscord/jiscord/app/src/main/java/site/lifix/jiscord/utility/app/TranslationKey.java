package site.lifix.jiscord.utility.app;

/*
 * Only add used translation keys as Java has a file length limit. As long as you verified the key exists, feel free
 * to add it. To verify, you can check the `current.js` file in the Discord-Datamining repository on GitHub.
 *
 * This is because there are over 15,000 translation keys.
 */
public enum TranslationKey {
    USER_SETTINGS
}
